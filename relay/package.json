{
  "name": "teams-wear-relay",
  "version": "0.1.0",
  "private": true,
  "description": "Netlify-Relay: haelt Microsoft-Graph-Subscriptions und pusht Teams-Nachrichten via FCM an die Wear-OS-App.",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "firebase-admin": "^12.3.0"
  },
  "devDependencies": {
    "@netlify/functions": "^2.8.1",
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0"
  }
}
